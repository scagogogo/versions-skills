# 约束

::: tip 能力域
解析与求值版本约束表达式（= != > >= < <= ^ ~ x，AND/OR）。
:::

## 📚 API 索引

| 符号 | 文档 |
|:--|:--|
| `ParseConstraint` | [`parse-constraint`](/sdk/api/parse-constraint) |
| `ParseConstraintSet` | [`parse-constraint-set`](/sdk/api/parse-constraint-set) |
| `ParseConstraintUnion` | [`parse-constraint-union`](/sdk/api/parse-constraint-union) |
| `NegateConstraint` | [`negate-constraint`](/sdk/api/negate-constraint) |
| `Match` | [`match-constraint`](/sdk/api/match-constraint) |
| `Match` | [`match-constraintset`](/sdk/api/match-constraintset) |
| `Match` | [`match-constraintunion`](/sdk/api/match-constraintunion) |
| `Satisfies` | [`satisfies-version`](/sdk/api/satisfies-version) |
| `Satisfies` | [`satisfies-constraintset`](/sdk/api/satisfies-constraintset) |
| `Satisfies` | [`satisfies-constraintunion`](/sdk/api/satisfies-constraintunion) |
| `String` | [`string-constraint`](/sdk/api/string-constraint) |
| `Len` | [`len-constraintset`](/sdk/api/len-constraintset) |

---

[← 返回 SDK 索引](/sdk/)
