# 单元测试覆盖率 100% 巩固与 CI 接入 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`
> Steps use checkbox (`- [ ]`) syntax.

**Goal:** 让"单元测试覆盖率 100%"在标准 `go test ./...` 流程与 CI 中都成立且可验证——把已有的 `scripts/verify_coverage.sh` 子进程覆盖率合并机制接入 CI、修复其可移植性与时序脆弱性，并跑通全量测试修复一切发现的问题。

**Architecture:** 现状：`go test -coverprofile ./...` 只收集主测试进程覆盖率，无法覆盖 `main()`、`os.Exit` 路径与 cobra Run 闭包 error 分支（这些只能在子进程触发）。项目已有 `scripts/verify_coverage.sh` + `internal/cli/testdriver/main.go`（`go build -cover` + `GOCOVERDIR` + `go tool covdata textfmt` + python3 按块取最大值合并），合并后真达 100%（1582 块全覆盖）。本 Plan 不新增测试机制，而是：①让脚本可移植（去掉硬编码 `go1.25.7`、适配 `-race`、随机端口）并加 100% 断言；②把 CI 的 coverage job 从 `go test -coverprofile` 切换为调用该脚本并断言 100%；③全量跑通修复一切问题。

**Tech Stack:** Go 1.25.5（CI）/ 1.25.7（本地），bash，python3，GitHub Actions，`go tool covdata`（Go 1.20+ 内置）

**Risks:**
- SSE 端口占用 + SIGINT 时序在 CI/`-race` 下可能 flaky → 缓解：端口改随机高位端口（`19876`→动态选取）、所有子进程 `|| true` 兜底、SIGINT 后 `wait` 带超时、`-race` 下 SIGINT 场景加 `|| true` 不阻断。
- 硬编码 `GO_BIN=go1.25.7` 在 CI 不可用 → 缓解：改用 `go`（setup-go 装的即符合 go.mod 要求的版本），本地通过 `GOTOOLCHAIN` 自动满足。
- python3 合并逻辑依赖 `go tool cover` 的 textfmt 行格式 → 缓解：Go 1.20+ 格式稳定，CI 用 1.25.5 满足。

---

### Task 1: 让 verify_coverage.sh 可移植、适配 -race、加 100% 断言

**Depends on:** None
**Files:**
- Modify: `scripts/verify_coverage.sh`（全文重写，保留策略骨架）

- [ ] **Step 1: 重写 verify_coverage.sh — 去硬编码、随机端口、-race 兼容、100% 断言**

文件: `scripts/verify_coverage.sh`（整体替换）

```bash
#!/usr/bin/env bash
# 验证全项目单元测试覆盖率 100%。
#
# 策略：
#   1. 各包 `go test -coverprofile` 收集父级覆盖率（成功路径 + 非 os.Exit 路径）。
#   2. internal/cli 的 os.Exit 路径 + cmd 的 main() 路径，通过 `go build -cover` 二进制
#      在带 GOCOVERDIR 的子进程中触发 os.Exit 时刷新覆盖率（go test 二进制不刷新）。
#   3. `go tool covdata textfmt` 转换 + 按块取最大值合并。
#   4. 断言合并 profile 中无 0 计数块，否则非 0 退出。
#
# 用法：
#   ./scripts/verify_coverage.sh            # 普通模式
#   ./scripts/verify_coverage.sh --race     # race 模式（CI 用）
set -euo pipefail

cd "$(dirname "$0")/.."

# 用 go 而非硬编码版本：CI 由 setup-go 装好符合 go.mod 的版本；
# 本地通过 GOTOOLCHAIN 自动拉取所需版本。
GO_BIN="${GO_BIN:-go}"
RACE_FLAG=""
if [[ "${1:-}" == "--race" ]]; then
  RACE_FLAG="-race"
fi

ROOT=$(pwd)
WORK=$(mktemp -d)
trap 'rm -rf "$WORK"' EXIT

echo "[1/6] 各包父级覆盖率"
$GO_BIN test $RACE_FLAG -coverprofile="$WORK/core.out" -coverpkg=github.com/scagogogo/versions-skills .
$GO_BIN test $RACE_FLAG -coverprofile="$WORK/verfmt.out" ./internal/verfmt/
$GO_BIN test $RACE_FLAG -coverprofile="$WORK/mcp.out" ./internal/mcp/
$GO_BIN test $RACE_FLAG -coverprofile="$WORK/cli.out" ./internal/cli/
$GO_BIN test $RACE_FLAG -coverprofile="$WORK/cmdv.out" ./cmd/versions/
$GO_BIN test $RACE_FLAG -coverprofile="$WORK/cmdm.out" ./cmd/versions-mcp/

echo "[2/6] internal/cli os.Exit 路径覆盖率"
$GO_BIN build -cover -covermode=atomic -tags cli_coverage_driver \
    -o "$WORK/cli_driver" ./internal/cli/testdriver
mkdir -p "$WORK/cli_cover"
CLI_DRIVER_GOCOVERDIR="$WORK/cli_cover" "$WORK/cli_driver" >/dev/null 2>&1 || true
$GO_BIN tool covdata textfmt -i="$WORK/cli_cover" -o "$WORK/cli_driver.out"

echo "[3/6] cmd 子进程覆盖率（cmd/versions + cmd/versions-mcp 的 main 路径）"
mkdir -p "$WORK/cmd_cover"
build_cover_bin() {
    local pkg="$1" out="$2"
    $GO_BIN build -cover -covermode=atomic -o "$out" "$pkg"
}
build_cover_bin ./cmd/versions "$WORK/versions_bin"
build_cover_bin ./cmd/versions-mcp "$WORK/versions_mcp_bin"

run_cover() {
    local bin="$1"; shift
    "$bin" "$@" >/dev/null 2>&1 || true
}
# cmd/versions 场景
GOCOVERDIR="$WORK/cmd_cover" run_cover "$WORK/versions_bin" parse 1.2.3
GOCOVERDIR="$WORK/cmd_cover" run_cover "$WORK/versions_bin" --help
GOCOVERDIR="$WORK/cmd_cover" run_cover "$WORK/versions_bin" unknown-cmd-xyz
# cmd/versions-mcp 场景
GOCOVERDIR="$WORK/cmd_cover" run_cover "$WORK/versions_mcp_bin" --help
GOCOVERDIR="$WORK/cmd_cover" run_cover "$WORK/versions_mcp_bin" --transport stdio < /dev/null
GOCOVERDIR="$WORK/cmd_cover" run_cover "$WORK/versions_mcp_bin" --transport bad-xyz
GOCOVERDIR="$WORK/cmd_cover" run_cover "$WORK/versions_mcp_bin" --unknown-flag-xyz
# sse 端口冲突 → ServeSSE.Start 返回 error → log.Fatalf
# 用随机高位端口避免与已占用端口冲突
SSE_PORT=$(python3 -c 'import socket; s=socket.socket(); s.bind(("",0)); print(s.getsockname()[1]); s.close()')
GOCOVERDIR="$WORK/cmd_cover" "$WORK/versions_mcp_bin" --transport sse --port "$SSE_PORT" >/dev/null 2>&1 &
SSE_PID=$!
sleep 1
GOCOVERDIR="$WORK/cmd_cover" run_cover "$WORK/versions_mcp_bin" --transport sse --port "$SSE_PORT"
kill $SSE_PID 2>/dev/null || true
wait $SSE_PID 2>/dev/null || true
# stdio + SIGINT → ServeStdio 因 ctx cancel 返回 error → log.Fatalf
# sleep 保持 stdin 打开，否则 stdin EOF 让 ServeStdio 提前返回 nil 退出，SIGINT 无的放矢
sleep 5 | GOCOVERDIR="$WORK/cmd_cover" "$WORK/versions_mcp_bin" --transport stdio >/dev/null 2>&1 &
STDIO_PID=$!
sleep 0.5
kill -INT $STDIO_PID 2>/dev/null || true
wait $STDIO_PID 2>/dev/null || true
$GO_BIN tool covdata textfmt -i="$WORK/cmd_cover" -o "$WORK/cmd_driver.out"

echo "[4/6] 合并所有覆盖率 profile"
# 合并：父级各包 + cli driver + cmd driver，按块取最大值
python3 - "$WORK" <<'PY'
import sys, os
work = sys.argv[1]
def load(p):
    blocks = {}
    mode = None
    if not os.path.exists(p): return blocks, mode
    for line in open(p):
        line = line.rstrip('\n')
        if line.startswith('mode:'):
            mode = line.split(':',1)[1].strip(); continue
        parts = line.rsplit(' ', 1)
        if len(parts) != 2: continue
        try: val = int(parts[1])
        except ValueError: continue
        blocks[parts[0]] = max(blocks.get(parts[0], 0), val)
    return blocks, mode
all_blocks = {}
mode = 'atomic'
for f in ['core.out','verfmt.out','mcp.out','cli.out','cmdv.out','cmdm.out']:
    b, m = load(f'{work}/{f}')
    if m: mode = m
    for k,v in b.items(): all_blocks[k] = max(all_blocks.get(k,0), v)
for f in ['cli_driver.out','cmd_driver.out']:
    b, m = load(f'{work}/{f}')
    for k,v in b.items():
        # 只保留本项目代码，排除 testdriver 自身
        if 'github.com/scagogogo/versions-skills/' in k and '/testdriver/' not in k:
            all_blocks[k] = max(all_blocks.get(k,0), v)
with open(f'{work}/merged.out','w') as o:
    o.write(f'mode: {mode}\n')
    for k in sorted(all_blocks):
        o.write(f'{k} {all_blocks[k]}\n')
# 断言：无 0 计数块
zero = [k for k,v in all_blocks.items() if v == 0]
if zero:
    print(f'❌ {len(zero)} 个代码块未覆盖:')
    for k in zero[:20]:
        print(f'  {k}')
    sys.exit(1)
print('merged profile written, 0 uncovered blocks')
PY

echo "[5/6] 覆盖率结果"
$GO_BIN tool cover -func="$WORK/merged.out" | tail -3

echo "[6/6] 复制合并 profile"
cp "$WORK/merged.out" "$ROOT/coverage_merged.out"
echo "已写入 coverage_merged.out"
echo "✅ 覆盖率 100% 验证通过"
```

- [ ] **Step 2: 验证脚本可移植性 — 用 go（非 go1.25.7）跑通**

Run: `cd /home/cc11001100/github/scagogogo/versions-skills && GO_BIN=~/go/bin/go1.25.7 GOTOOLCHAIN=local bash scripts/verify_coverage.sh`
Expected:
  - Exit code: 0
  - Output contains: "✅ 覆盖率 100% 验证通过"
  - Output contains: "total" 且 "100.0%"

- [ ] **Step 3: 验证 --race 模式跑通**

Run: `cd /home/cc11001100/github/scagogogo/versions-skills && GO_BIN=~/go/bin/go1.25.7 GOTOOLCHAIN=local bash scripts/verify_coverage.sh --race`
Expected:
  - Exit code: 0
  - Output contains: "✅ 覆盖率 100% 验证通过"
  - Output does NOT contain: "DATA RACE" 或 "WARNING: DATA RACE"

- [ ] **Step 4: 提交**
Run: `git add scripts/verify_coverage.sh && git commit -m "test: verify_coverage.sh 改用 go、适配 -race、随机端口、加 100% 断言"`

---

### Task 2: CI 接入 verify_coverage.sh 并断言 100%

**Depends on:** Task 1
**Files:**
- Modify: `.github/workflows/go-test.yml:33-50`（coverage job）

- [ ] **Step 1: 修改 CI coverage job — 调用 verify_coverage.sh 并断言 100%**
文件: `.github/workflows/go-test.yml:33-50`（替换 coverage job 整段）

```yaml
  coverage:
    name: Coverage 100%
    needs: unit-test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25.5'
          check-latest: true

      - name: Verify 100% coverage (incl. subprocess os.Exit / main paths)
        run: |
          bash scripts/verify_coverage.sh
          # 兜底二次断言：合并 profile 中无 0 计数块
          if awk '$1!="mode:" && $NF==0 {found=1; print "  未覆盖:", $0} END{exit found}' coverage_merged.out; then
            echo "✅ 合并 profile 无 0 计数块"
          else
            echo "❌ 仍存在未覆盖块"
            exit 1
          fi

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-merged
          path: coverage_merged.out
```

- [ ] **Step 2: 本地预演 CI 行为 — 模拟 CI 用 go（非 go1.25.7）跑脚本**

Run: `cd /home/cc11001100/github/scagogogo/versions-skills && GO_BIN=~/go/bin/go1.25.7 GOTOOLCHAIN=local bash scripts/verify_coverage.sh && awk '$1!="mode:" && $NF==0 {found=1; print "未覆盖:", $0} END{exit found}' coverage_merged.out`
Expected:
  - Exit code: 0
  - Output contains: "✅ 覆盖率 100% 验证通过"

- [ ] **Step 3: 提交**
Run: `git add .github/workflows/go-test.yml && git commit -m "ci: coverage job 改用 verify_coverage.sh 并断言 100%（含子进程路径）"`

---

### Task 3: 全量跑通测试并修复一切发现的问题

**Depends on:** Task 1, Task 2
**Files:**
- Modify: 视发现的问题而定（无预设文件）

- [ ] **Step 1: 跑标准 go test 全量 — 确认无失败**

Run: `cd /home/cc11001100/github/scagogogo/versions-skills && GO_BIN=~/go/bin/go1.25.7 GOTOOLCHAIN=local ~/go/bin/go1.25.7 test ./...`
Expected:
  - Exit code: 0
  - Output contains: "ok" 且不包含 "FAIL"

- [ ] **Step 2: 跑 go vet + gofmt 检查 — 确认代码质量**

Run: `cd /home/cc11001100/github/scagogogo/versions-skills && GOTOOLCHAIN=local ~/go/bin/go1.25.7 vet ./... && GOTOOLCHAIN=local ~/go/bin/go1.25.7 fmt ./...`
Expected:
  - Exit code: 0
  - gofmt 输出为空（无格式问题）

- [ ] **Step 3: 跑 -race 全量测试 — 确认无数据竞争**

Run: `cd /home/cc11001100/github/scagogogo/versions-skills && GOTOOLCHAIN=local ~/go/bin/go1.25.7 test -race ./...`
Expected:
  - Exit code: 0
  - Output does NOT contain: "DATA RACE"

- [ ] **Step 4: 最终确认覆盖率 100% — 跑 verify_coverage.sh**

Run: `cd /home/cc11001100/github/scagogogo/versions-skills && GO_BIN=~/go/bin/go1.25.7 GOTOOLCHAIN=local bash scripts/verify_coverage.sh`
Expected:
  - Exit code: 0
  - Output contains: "total" 且 "100.0%"
  - Output contains: "✅ 覆盖率 100% 验证通过"
  - 合并 profile 中无 0 计数块

- [ ] **Step 5: 提交（若有修复）**
Run: `git add -A && git commit -m "test: 全量测试跑通，覆盖率 100%（含子进程 os.Exit / main 路径）" || echo "无变更需提交"`
